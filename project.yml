name: MP4M
options:
  bundleIdPrefix: com.ktam
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
  xcodeVersion: "16.0"
  defaultConfig: Debug

configs:
  Debug: debug
  Release: release

targets:
  MP4M:
    type: application
    platform: macOS
    deploymentTarget: "14.0"

    info:
      path: MP4M/Resources/Info.plist
      properties:
        CFBundleName: MP4M
        CFBundleDisplayName: MP4M
        CFBundleIdentifier: com.ktam.MP4M
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        NSPrincipalClass: NSApplication
        NSHumanReadableCopyright: "Copyright © 2026 ktam"
        LSMinimumSystemVersion: "14.0"

    entitlements:
      path: MP4M/Resources/MP4M.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.files.user-selected.read-only: true

    sources:
      - path: MP4M
        excludes:
          - "**/.DS_Store"
          - "**/MP4M.entitlements"
          - "**/Info.plist"
          - Bridge
      - path: MP4M/Bridge
        includes:
          - "MXDRVGBridge.mm"
      - path: Vendor/gamdx/jni/mxdrvg
        includes:
          - "so.cpp"
      - path: Vendor/gamdx/jni/pcm8
        includes:
          - "*.cpp"
      - path: Vendor/gamdx/jni/downsample
        includes:
          - "downsample.cpp"
      - path: Vendor/lzx
        includes:
          - "lzx.cpp"
      - path: Vendor/opm
        includes:
          - "*.cpp"

    settings:
      BRIDGING_HEADER: MP4M/Bridge/MP4M-Bridging-Header.h
      HEADER_SEARCH_PATHS: $(SRCROOT) $(SRCROOT)/Vendor $(SRCROOT)/Vendor/gamdx/jni $(SRCROOT)/MP4M/Bridge
      USER_HEADER_SEARCH_PATHS: $(SRCROOT)/MP4M/Bridge
      CLANG_CXX_LANGUAGE_DIALECT: gnu++17
      CLANG_CXX_LIBRARY: libc++
      GCC_C_LANGUAGE_DIALECT: gnu17
      WARNING_CFLAGS: -Wall -Wextra
      CLANG_ENABLE_OBJC_ARC: YES
      SWIFT_OBJC_BRIDGING_HEADER: MP4M/Bridge/MP4M-Bridging-Header.h
      FRAMEWORK_SEARCH_PATHS: $(inherited)
      LINK_FLAGS: -framework AVFoundation -framework AppKit -framework CoreGraphics

    dependencies: []

    resources:
      - path: MP4M/Resources
        includes:
          - "*.ttf"
          - "*.dat"
          - "*.xcassets"
